{
  "name": "portfolio-mfe",
  "private": true,
  "scripts": {
    "dev": "npx pnpm --parallel -r run dev",
    "build:remotes": "npx pnpm --filter react-mfe run build && pnpm --filter vue-mfe run build",
    "build": "npx pnpm run build:remotes && npx pnpm --filter shell run build",
    "preview": "pnpm --parallel -r run preview",
    "install:apps": "npm i --prefix shell && npm i --prefix react-mfe && npm i --prefix vue-mfe"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.4.5"
  }
}
