{
  "name": "portfolio-mfe",
  "homepage": "https://brianjvarley.github.io",
  "private": true,
  "scripts": {
    "predeploy:gh": "pnpm build && cp -r react-mfe/dist shell/dist/react-mfe && cp -r vue-mfe/dist shell/dist/vue-mfe && touch shell/dist/.nojekyll",
    "deploy:gh": "gh-pages -d shell/dist -r https://github.com/BrianJVarley/brianjvarley.github.io.git -b main",
    "dev": "npx kill-port 3000 3001 3002 && npx pnpm --parallel -r run dev",
    "dev:clean": "lsof -ti tcp:3000,3001,3002 -sTCP:LISTEN | xargs kill -9 2>/dev/null || true",
    "build:remotes": "npx pnpm --filter react-mfe run build && pnpm --filter vue-mfe run build",
    "build": "npx pnpm run build:remotes && npx pnpm --filter shell run build",
    "preview": "pnpm --parallel -r run preview",
    "install:apps": "npm i --prefix shell && npm i --prefix react-mfe && npm i --prefix vue-mfe"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.5"
  }
}
